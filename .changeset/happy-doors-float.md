---
'hostd': patch
---

Fixed the address configuration description to say ports "may" be configured instead of "must" since default ports are used if not specified. Closes https://github.com/SiaFoundation/hostd/issues/908
